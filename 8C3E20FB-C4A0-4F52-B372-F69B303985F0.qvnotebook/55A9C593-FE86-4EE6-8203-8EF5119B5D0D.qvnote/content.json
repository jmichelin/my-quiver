{
  "title": "toCamelCase",
  "cells": [
    {
      "type": "code",
      "language": "javascript",
      "data": "const toCamelCase = (str, delimeter) => {\n  return str.split(delimeter).map((word, index)=>{\n    return index === 0 ? word : word.charAt(0).toUpperCase() + word.substr(1, word.length);\n  }).join('');\n};"
    }
  ]
}